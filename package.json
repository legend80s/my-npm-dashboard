{
  "name": "my-npm-dashboard",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "bin": {
    "marmot": "src/bin/index.js"
  },
  "scripts": {
    "tauri": "tauri",
    "typecheck": "tsgo --noEmit",
    "start": "node src/bin/index.js",
    "dev:api": "cd src/server && echo Y | pnpm i && node --watch ./dev-server",
    "dev:live": "live-server ./src --port=1123 --open=\"?limit=3&username=antfu\"",
    "dev": "concurrently --kill-others --names \"LIVE,API\" \"pnpm dev:live\" \"pnpm dev:api\""
  },
  "devDependencies": {
    "@tauri-apps/cli": "^2",
    "@types/node": "^26.1.1",
    "concurrently": "^10.0.4",
    "live-server": "^1.2.2"
  },
  "dependencies": {
    "chart.js": "^4.5.1",
    "sourdough-toast": "^0.3.0"
  }
}
